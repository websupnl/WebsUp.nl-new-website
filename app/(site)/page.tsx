// De homepage staat in app/page.tsx (root level).
// Dit bestand is een lege placeholder zodat Next.js geen missing-page fout geeft
// voor de (site) route group. app/page.tsx heeft voorrang voor de "/" route.
//
// Verwijder dit bestand als je Next.js dit toestaat (sommige versies accepteren
// een route group zonder een page.tsx op root niveau).

export { default } from '@/app/page'
